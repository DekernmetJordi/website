---
type Props = {
  category: string;
};

const { category } = Astro.props;
---

<a
  href={`/category/${category}`}
  class="text-brand-copper rounded-3xl uppercase text-xl p-2 border-2 border-brand-copper whitespace-nowrap"
  >{category}</a
>
